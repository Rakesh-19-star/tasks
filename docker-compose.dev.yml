services:
  tasks-api:
    build: .
    container_name: tasks-api-dev
    env_file:
      - .env
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - /app/node_modules
      - tasks-db:/app/database
    command: npm run dev

volumes:
  tasks-db: